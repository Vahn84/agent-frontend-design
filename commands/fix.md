---
description: Analyze FIXLOG for a screen, propose fixes, confirm, implement, re-validate
argument-hint: "<screen-name>"
---

Run `agents/FIX.md` workflow on screen $ARGUMENTS.

Prereq: screen built; at least one `validate` run has produced `FIXLOG.md` in the project root.
