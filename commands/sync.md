---
description: Sync design-contract ↔ code
---

Run `agents/SYNC.md` workflow. Re-audits Figma, diffs vs current contract, rebuilds impacted files, validates.

Prereq: `design-contract/` exists + codebase exists + `FIGMA_PAT` set (REST client smoke-checked).
