---
description: Create new app (no Figma)
argument-hint: "[description]"
---

Run `agents/CREATE-APP.md` workflow. No Figma required.

Input: $ARGUMENTS (optional app description).

Setup questions (batched): framework, project name, library, theme seed.
