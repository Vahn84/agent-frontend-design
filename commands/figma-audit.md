---
description: Audit Figma file → design-contract/
argument-hint: "[figma-url]"
---

Read `agents/FIGMA-TO-CODE.md` Phase A gate OR run standalone audit via `agents/AUDIT.md`.

Prereq: `FIGMA_PAT` set (REST client smoke-checked via `client.me()`). Ask PAT + framework + project name if not provided.

Input: $ARGUMENTS (Figma URL or node URL).
