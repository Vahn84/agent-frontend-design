---
description: Replace custom components with library equivalents (PrimeReact/MUI/etc) preserving Figma fidelity
argument-hint: "<screen-name>"
---

Run `agents/WIRE-LIBRARY.md` workflow on screen $ARGUMENTS (or all screens if omitted).

Prereq: `meta.yml.library.name` must be set. Screen + custom components built. Library installed as project dep.
