---
description: Validate code fidelity vs Figma
argument-hint: "[--fast | --full]"
---

Run `agents/VALIDATE-WORKFLOW.md`. Default `--full` (includes pixel diff). `--fast` skips pixel diff for quick iteration.

Prereq: `design-contract/` + codebase + dev server bootable.
