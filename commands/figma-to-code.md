---
description: Generate code from design-contract/
argument-hint: "[figma-url]"
---

Run `agents/FIGMA-TO-CODE.md` workflow. If `design-contract/` missing, run audit phase first.

Input: $ARGUMENTS (optional Figma URL).

Setup questions (batched): framework, project name, FIGMA_PAT, library.
